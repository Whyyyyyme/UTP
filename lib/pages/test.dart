import 'package:flutter/material.dart';

class cobatest extends StatelessWidget {
  const cobatest({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
